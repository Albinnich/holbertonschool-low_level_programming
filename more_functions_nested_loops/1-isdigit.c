#include "main.h"
#include <stdio.h>

/**
 * _isdigit - function that check for a digit from 0 through 9
 * (0 through 9) tested character
 * Return: returns 1 if c is a digit, 0 if not
 */

int _isdigit(int c);

{

	if (c == "%d")
	{
		return (1);
	}
	return (0);
}
