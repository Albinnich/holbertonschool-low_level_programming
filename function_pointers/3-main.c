#include <stdlib.h>

/**
 * main - function
 * @atoi: to convert arguments
 * Return: nothing
 */

int main(void)

	int operator;
	int num1;
	int num2;
	int atoi;

{
	if (argc != 0)
		printf("Error\n");
	return (98);
}
if (operator != "+" && != "-" && != "*" && != "/" && != "%")
{
	printf("Error\n");
	return (99);
}
if (num1 / 0 || num1 % 0 || num2 / 0 || num2 % 0)
	printf("Error\n");
	return (100);
	}

{
atoi(num1 int);
atoi(num2 int);
}
